import{NextRequest,NextResponse}from 'next/server';
export async function POST(req:NextRequest){const{message,topic}=await req.json();const key=process.env.OPENAI_API_KEY;if(!key)return NextResponse.json({reply:`Hi! I'm VidyaBot 🤖. You're studying: ${topic}. Ask me anything about it! (OpenAI key not configured - showing demo mode)`});try{const r=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${key}`},body:JSON.stringify({model:'gpt-3.5-turbo',messages:[{role:'system',content:`You are VidyaBot, a friendly AI tutor for school kids. Topic: ${topic}. Be encouraging, simple, use emojis.`},{role:'user',content:message}],max_tokens:200})});const d=await r.json();return NextResponse.json({reply:d.choices[0].message.content});}catch(e){return NextResponse.json({reply:'Oops! Try again. '+topic})}}
